---
description: how to run and test the android application
---

1. Ensure you have an Android device connected or an emulator running.
2. Run the following command to build and install the app:
// turbo
```cmd
gradlew installDebug
```
3. To run unit tests, use:
// turbo
```cmd
gradlew test
```
